# App-Q Web

Frontend placeholder. The current local operator UI lives in `apps/operator/streamlit_app.py`.

Recommended first implementation:

- Next.js or Vite React
- Research brief form
- Clarifying question step
- Persona panel view
- Interview progress view
- Report view with evidence chains

The frontend should read `APP_DISPLAY_NAME` from configuration so the codename can change later without refactoring the codebase.
