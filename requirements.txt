fastapi==0.141.1
uvicorn==0.34.0
pydantic==2.10.5
pydantic_core==2.27.2
starlette==1.3.1
requests==2.34.2
python-dotenv==1.2.2
PyYAML==6.0.3
Jinja2==3.1.6
Markdown==3.10.2

# Database
psycopg2-binary==2.9.12
pgvector==0.4.2

# Async / Queue
celery==5.6.3
redis==8.1.0
slowapi==0.1.9

# LLM API
openai==3.13.0

# Analytics / Viz
# R8-8: pandas / numpy / altair / plotly kaldırıldı — kod tabanında hiç referans
# yoktu (analiz katmanı saf Python; grafikler frontend'de ve rapor SVG'lerinde).

# Orchestration
langgraph==1.2.11
langchain-core==1.6.3

# Monitoring (SENTRY_DSN yoksa devre dışı)
sentry-sdk[fastapi]==2.69.1

# Reporting
xhtml2pdf==0.2.19

# Utils
tenacity==9.1.4
jsonschema==4.26.0
pillow==12.3.0
protobuf==5.29.6
limits==5.8.0

# NOT (R8-8 sonrası): pyarrow ve GitPython kaldırıldı — kod tabanında referans yok.
# Opsiyonel/isteğe bağlı paketler için requirements-optional.txt dosyasına bakın.
