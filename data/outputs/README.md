# Outputs

Generated reports and eval results are written here during local runs.

These files are ignored by git by default because they may contain customer or model output data.

Tracked placeholders:

- `.gitkeep`
- `README.md`
